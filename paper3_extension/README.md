# Paper 3 Extension Workspace

This directory is reserved for ES-FA system-level extensions such as
adaptive execution and higher-level spiking-attention experiments.

Current implementation focus:
- adaptive policy integration in `deployment/runtime`
- system routing and CLI integration in `system_layer` and `deployment/cli_interface`

