# Local Tool Runs

This folder is for generated tool artifacts that should not sit in the repo
root. Nothing here is hand-written source code.

Current archive:

- `root_artifacts_20260505/`  
  Vivado/xsim logs, backup journals, `.Xil`, and `xsim.dir` files that were
  produced during earlier local validation runs.

